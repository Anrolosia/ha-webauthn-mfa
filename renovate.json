{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "helpers:pinGitHubActionDigests"
  ],
  "packageRules": [
    {
      "description": "Keep workflow action bumps together in a single PR.",
      "matchManagers": ["github-actions"],
      "groupName": "github actions"
    },
    {
      "description": "The Home Assistant validation actions track their upstream branch on purpose so new rules apply on the next run; do not pin them to a digest.",
      "matchManagers": ["github-actions"],
      "matchPackageNames": ["home-assistant/actions", "hacs/action"],
      "pinDigests": false
    },
    {
      "description": "Keep the test and lint dependency bumps together in a single PR.",
      "matchManagers": ["pip_requirements"],
      "groupName": "python dependencies"
    },
    {
      "description": "Keep the dev container image bumps together in a single PR.",
      "matchManagers": ["docker-compose"],
      "groupName": "dev container images"
    },
    {
      "description": "webauthn 3.x needs pyOpenSSL>=26.3.0, which Home Assistant does not ship yet. Revisit when the HA constraint moves.",
      "matchManagers": ["homeassistant-manifest", "pip_requirements"],
      "matchPackageNames": ["webauthn"],
      "allowedVersions": "<3.0.0"
    }
  ]
}
